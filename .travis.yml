language: node_js
node_js:
  - "10"
dist: trusty
sudo: required
branches:
  only:
  - travis-setup
before_script:
  - npm install -g @angular/cli
script:
- ng lint
- ng build --prod --base-href https://github.com/DSonesson/MedCom-TDDC88
